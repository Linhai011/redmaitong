# 红脉通 · 前后端打通部署说明

把「前端原型 + Node.js 后端 + MySQL」三件套跑起来，让前端真正读写数据库。

## 架构回顾

```
浏览器  http://VM_IP:3000
   │  fetch('/v1/...')  同源，无跨域
   ▼
Node.js + Express 后端（跑在 Linux VM 上，127.0.0.1:3000）
   │  mysql2 连接池
   ▼
MySQL redmaitong（VM 本机 127.0.0.1:3306）
```

- 后端同时**托管前端**（index.html / css / js），所以浏览器访问 `http://VM_IP:3000` 就能看到页面。
- 后端连的是 VM **本机** MySQL（`127.0.0.1`），**不需要**给 MySQL 开远程访问、也不用改 `bind-address`。

---

## 一、准备：在 VM 上装 Node（若还没装）

```bash
node -v        # 有输出且 >= 16 就跳过本节
```

> **Node 版本说明**：后端只需 **Node ≥ 16**（代码只用了 CommonJS `require` + async/await，无新语法），Node 20/18 不是硬性要求。装不上最新版就用下面任意一条回退，别卡在这一步。

**先换 npm 国内源（重要，后面 `npm install` 也会用到）：**

```bash
npm config set registry https://registry.npmmirror.com
```

**主路径：NodeSource（能连上外网就用它）**

Ubuntu / Debian：
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

CentOS / RHEL：
```bash
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs
```

装完再 `node -v`、`npm -v` 验证。

---

### 装不上 Node 20？用下面几条回退

国内访问 `deb.nodesource.com` / `raw.githubusercontent.com` 慢或超时（GPG key、网络都在这卡），是装不上的主要原因。下面几条走国内镜像，不碰 apt 源、不碰 GPG。

**回退 A：官方二进制包 + 国内镜像（推荐，最省事）**

```bash
cd /tmp
# 清华镜像（打不开就换下面淘宝镜像那行）
wget https://mirrors.tuna.tsinghua.edu.cn/nodejs-release/v18.20.4/node-v18.20.4-linux-x64.tar.xz
# 淘宝镜像备选：
# wget https://npmmirror.com/mirrors/node/v18.20.4/node-v18.20.4-linux-x64.tar.xz
tar -xJf node-v18.20.4-linux-x64.tar.xz
sudo mv node-v18.20.4-linux-x64 /usr/local/node
echo 'export PATH=/usr/local/node/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
node -v          # 应显示 v18.20.4
```

**回退 B：nvm（gitee 镜像装，能装任意版本）**

```bash
git clone https://gitee.com/mirrors/nvm.git ~/.nvm
cd ~/.nvm && source nvm.sh
export NVM_NODEJS_ORG_MIRROR=https://npmmirror.com/mirrors/node
nvm install 18 && nvm use 18
node -v
```

> ⚠️ **CentOS 7 用户注意**：Node 18 及以后要求系统 glibc ≥ 2.28，CentOS 7 只有 2.17，装上也跑不起来（报 `GLIBC_2.28 not found`）。CentOS 7 只能装 **Node 16**：`nvm install 16 && nvm use 16`。Ubuntu/Debian 不受影响。

---

## 二、上传项目到 VM

用 FinalShell 的**文件管理（SFTP）**，把整个项目目录传到 VM，例如放到 `/opt/redmaitong`。

最小需要这些内容（`database/` 目录可以不传，DB 已经建好了）：

```
/opt/redmaitong
├── index.html
├── css/
├── js/
│   ├── api.js
│   ├── render.js
│   └── app.js
└── backend/
    ├── server.js
    ├── db.js
    ├── auth.js
    ├── map.js
    ├── routes/…
    ├── package.json
    └── .env.example
```

> 提示：FinalShell 左侧「文件管理」面板可以直接拖拽上传整个文件夹。

---

## 三、装依赖 + 配置

```bash
cd /opt/redmaitong/backend
npm install
cp .env.example .env
vi .env       # 改下面两项
```

`.env` 里需要改的只有 **`DB_PASSWORD`** 和 **`JWT_SECRET`**：

```ini
DB_HOST=127.0.0.1        # 不用改，MySQL 就在本机
DB_PORT=3306             # 不用改
DB_USER=root             # 或你建的专用账号
DB_PASSWORD=你的MySQL密码   # ← 必改
DB_NAME=redmaitong       # 不用改

PORT=3000                # 不用改

JWT_SECRET=随便一长串随机字符   # ← 必改（生产必须随机）
```

> 注意：`.env` 里的密码不会被提交，也不该被任何人看到；后端只在服务器上读它，浏览器永远拿不到。

---

## 四、启动

**前台调试（推荐第一次用，能直接看日志）：**
```bash
cd /opt/redmaitong/backend
node server.js
```
看到 `红脉通后端已启动：http://localhost:3000` 就成功了。

**后台运行（关掉 FinalShell 也能继续跑）：**
```bash
nohup node server.js > app.log 2>&1 &
```

---

## 五、访问

浏览器打开：

```
http://VM_IP:3000
```

**如果打不开，多半是防火墙没放行 3000 端口：**

```bash
# Ubuntu (ufw)
sudo ufw allow 3000
# CentOS (firewalld)
sudo firewall-cmd --add-port=3000/tcp --permanent && sudo firewall-cmd --reload
```

云服务器的话，还要在**安全组**里放行 3000/tcp。

---

## 六、登录测试账号

`02_seed.sql` 里灌了 5 个账号，**密码都是 `password`**：

| 账号 | 角色 | 所属机构 |
| :--- | :--- | :--- |
| `admin` | 平台管理员 | 无（平台） |
| `zhangguan` | 机构管理员 | XX省综合档案馆 |
| `liguan` | 机构管理员 | XX市革命历史纪念馆 |
| `chenlaoshi` | 机构普通用户 | XX大学马克思主义学院 |
| `wangxiaoming` | 个人用户 | 无 |

建议先用 **`zhangguan` / `password`** 登录，体验最完整（能上传、能审批、能看个人中心）。

---

## 七、验证清单（端到端）

**1. 先确认后端 + 数据库连通：**
```bash
curl http://127.0.0.1:3000/v1/health
# 预期：{"code":200,"msg":"ok","data":{"db":"ok"},...}

curl http://127.0.0.1:3000/v1/org/list
# 预期：返回 5 家机构，字段是 camelCase（orgId / orgName / resourceCount）
```

**2. 浏览器验证（http://VM_IP:3000）：**

| 操作 | 预期 |
| :--- | :--- |
| 打开首页 | 机构卡、热门资源、数据统计块都从数据库渲染（数量 = 库里的真实值） |
| 资源库点「筛选检索」 | 按机构/类型/等级真实过滤（走 `/resource/search`） |
| 登录 `zhangguan`/`password` | 头部头像变「z」；进个人中心看到 4 条收藏 |
| 资源详情点「申请跨机构调取」→ 提交 | `share_auth` 表多一条记录 |
| 共享授权页「通过/驳回」 | 状态变更 + 申请人收到通知 + 写操作日志 |
| 详情页点「申请跨机构调取」前的浏览 | `resource.view_count` +1 |
| 机构入驻表单提交 | `org` 表多一条 `status=0` |
| 上传资源（选个文件） | `resource`(待审核) + `resource_file` 各多一条，文件落到 `backend/uploads/` |

> 想看数据库是否真的变了，在 MySQL 里 `SELECT * FROM share_auth ORDER BY auth_id DESC;` 等即可。

---

## 八、常见问题排查

**Q1：`/v1/health` 返回「数据库连接失败」**
→ 检查 `.env` 的 `DB_PASSWORD`、`DB_NAME` 是否对；用 `mysql -u root -p redmaitong` 手动登一下确认。

**Q2：浏览器打不开 `http://VM_IP:3000`**
→ 先看 VM 本机 `curl http://127.0.0.1:3000/v1/health` 通不通；通了就是防火墙/安全组没放行 3000 端口。

**Q3：登录报「用户名或密码错误」**
→ 确认账号来自 `02_seed.sql`、密码是 `password`；若你自己改过密码，用改后的。密码是 bcrypt 比对，明文对不上就报这个错。

**Q4：页面能开但数据区显示「后端未连接，当前为静态演示模式」**
→ 这是**降级提示**，说明前端没连上后端。检查是不是用 `file://` 双击打开 index.html 了——请务必用 `http://VM_IP:3000` 访问。若确需 `file://` 打开，改 `js/api.js` 里的 `API_BASE` 为后端绝对地址。

**Q5：ECharts 图表空白**
→ 仪表盘用的是 ECharts 的 CDN（`cdn.jsdelivr.net`），VM 要能访问外网；离线环境下图表会空，但其它数据不受影响。

---

## 九、与 04_advanced.sql 的关系

本次后端是**自包含**的，只跑过 `01 + 02` 就能用：

- **检索**：默认用 `LIKE`（不需要全文索引）。
- **下载/浏览计数**：后端在事务里**显式 UPDATE**（不依赖触发器）。
- **审批**：后端在事务里实现（不依赖存储过程）。

如果之后再跑 `04_advanced.sql`，注意两点：

1. **全文索引**：`resource.js` 的 `search` 里注释写了怎么把 `LIKE` 换成 `MATCH…AGAINST`，能走倒排索引、按相关度排序。
2. **触发器会重复计数**：`04` 里的 `trg_download_count` / `trg_browse_count` 会在插入下载/浏览记录时再 +1，和后端显式 UPDATE **叠加会 +2**。二者**二选一**——要么靠触发器（删掉后端 UPDATE），要么靠后端显式 UPDATE（别建触发器）。
